# Examples

Sample prompt JSON files may live here for documentation only.

**Production prompts** go in dated folders:

```text
data/prompts/YYYY-MM-DD/YYYYMMDD-NNN.json
```

Example files under `examples/` are **not** indexed by `scripts/generate_readme.py` only if they lack required fields — prefer keeping real prompts out of this folder.
